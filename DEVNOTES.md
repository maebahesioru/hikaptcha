# 開発メモ

設計判断の根拠と実測記録。ドキュメント本体(README.md)には結論だけを書き、
経緯と測定値はここに残す。

## 出題生成の性能

形式を6種類に増やした直後は、フィルタの通過率が低く出題生成が遅かった。

- 20枚取得時の段階0通過は中央値5枚(80%が空振り)、60枚なら中央値10枚(20%)。取得時間は196ms→274ms
- 対策として取得枚数を60枚(不足時は100枚で1回だけ取り直し)、候補条件を「グリッドに入れる枚数」基準に緩和
- 結果: 中央値 2358ms → 1142ms、試行 4.3 → 2.6回/件
- 失敗理由は `/api/health` の `stats`(fShortBatch/fNoTag/fPool 等)で切り分けられる

## タグの選別

- 候補タグの約半数はOCRノイズ相当(使用回数53以下)。`TAG_MIN_USAGES=150` で除外しても候補は十分残る
- タグのカテゴリはすべて `default`、含意・提案も空で、人間が付けたタグとの区別はできない
- タグ配列の並び順は使用回数の降順で、信頼度の情報は含まれない

## タグと画像の対応(誤付与・誤漏れ)

visionによる採点で確認した失敗型と対策:

| 失敗型 | 例 | 対策 |
|---|---|---|
| ダミーが正解と同じに見える | 「青いシャツ」: 正解2枚に対しvisionは7枚を該当と判定(同じキャラの紺Tシャツ9枚のうちタグは2枚のみ) | 見分けやすさフィルタ(正解同士のタグ類似度とダミーの類似度を比較) |
| ダミーが画像として似すぎている | 「長袖」: 正解2枚に対しvisionは8枚と判定(同人物の雪山ジャケット連続写真) | 視覚フィルタ(8x8グレースケール署名で比較、1回のffmpeg呼び出しで9枚処理) |
| タグの付与漏れ | 「カーディガン」を着ているのにタグが無い画像 | 関連タグ(文字列の包含・3文字以上の一致)を持つ画像を正解側に昇格 |
| タグの誤付与 | 電車の画像に「バッグ」 | 正解候補の一貫性フィルタ+共起タグによる確度順の選別 |

共起タグの検証(「眼鏡」): タグ無しで共起スコア上位の6枚のうち5枚に実際に眼鏡が写っていた。
タグ有りで共起スコア0の6枚のうち4枚には写っていなかった。この結果をスコア順の候補選別に利用している。

タイル単位の誤り率(120問のvision採点): 18.5% → フィルタ群の導入後 14.8〜16.7%。
12件ずつの比較のため差は誤差の範囲で、支配的な残り誤差はタグの付与漏れ。

## 出題タグの多様性

同一タグの再出率は40%(120問で異なり78種、礼服×10・茶髪×8)だった。

- 原因1: 重み `n² × concreteness(usages)` が人気タグを最大24倍優遇していた → `n^1.5 × √concreteness` に平坦化
- 原因2: バッチが巨大シリーズに偏り、どのバッチにも出るタグが存在する
- 対策: 直近に使ったタグとその類似タグ(短いタグは末尾の漢字が同じものも含む)を15分間避ける。
  候補が全部直近の場合は「最後に使ってから一番古いタグ」を選ぶ(重み付き抽選に戻すと再出が再発した)
- 結果: 再出率 40% → 3.3%(80問で異なり89種、1回だけ出たタグが97%)

タグ語彙の実測:

| 範囲 | タグ総数 | 名前フィルタ通過 |
|---|---|---|
| booru全体 | 374,884 | — |
| 使用回数150〜3000 | 1,413 | 316 |
| 使用回数80〜3000 | 2,191 | 525 |

下限を80に広げると使えるタグは増えるが、タイル誤り率が16.7%→31.9%、完全一致が6/12→0/8に悪化したため
既定は150のままにしている(`TAG_MIN_USAGES` で変更可能)。

不適切語の混入も実測で確認し、除外リストを追加した(使用回数150以上の帯域に「多性器」「二重肛門」
「セックストイ」、80〜149の帯域に「ヌード」「セックス」「性器」「ディルドー」が存在した)。

## 画像の見え方

タイルは3:2、配信画像は長辺640px。正方形+containで上下に40%の余白が出て被写体占有率が46%まで
落ちていたため、3:2に変更して占有率を改善した。クロップ・反転・回転は廃止している
(反転は適応的な攻撃者に対して距離0.2で無効、視覚的な判別にも悪影響があった)。

## 攻撃耐性(実測)

- 素朴なボット(画像URLを直接取得して総当たり): 5回中3回突破 → 画像URLの不透明化とチケット束縛で0/5に
- 画像を取得しない直叩き: 拒否
- 即答: 拒否
- 別チケットでのトークン消費: 拒否
- PoWのコスト: sha256比 1試行あたり約780倍(scrypt N=1024, r=8)
- 知覚ハッシュによる照合は原理的に防げない(距離約6で一致する)

## 埋め込み検証

別オリジンの模擬サイト(:3200)で18項目を検証(タイル9枚の描画、cross-originでの画像取得、
`/api/challenge` の取得、`onSolved` の受信、トークンのワンタイム性、チケット無しの消費拒否など)。

この検証で見つけた不具合: 画像がブラウザキャッシュに載っている2回目以降のアクセスや、
一瞬で解ける出題で、正当な回答が `MIN_SOLVE_MS`(1500ms)を割って拒否されていた(3回に1回失敗)。
ウィジェットが `minMs` まで待ってから送信するようにして解決し、検証が3回連続18/18で安定した。

## レート制限

既定では無効(`IP_QUOTA=0`)。有効化した場合の挙動は、固定窓方式からトークンバケット方式に変更し、
429時に `Retry-After` と残秒を返す。混雑時は拒否ではなく順番待ちに倒す設計のため、
`MAKE_CONCURRENCY` を超えた生成要求は無制限に待機する。

## OCR・文字起こしタグの検証(不採用)

「文字を読ませる認証」に使えるかを検証したが、booruのタグは内容を表すもので、画面に写っている文字
ではなかった。

- 「日本語のテキスト」タグ(20,886枚): タグ付き6枚中4枚に文字があり、タグ無し6枚中3枚にも文字があった
- 「どうもヒカキンです」(185枚): 画像に写っていたのは別の文字だった
- 「チャンネル登録」(647枚): タグ付き5枚を含む10枚で、その文字は写っていなかった
- OCR断片タグ(「チャンネル登録してわ」等)は実際の画面文字の転記だが、すべて使用回数1で語彙にならない

自前OCR(YomiToku)での文字抽出は可能(GPUで0.6秒/枚)だが、タイルサイズ(300x200)に縮小すると
文字が判読できなくなるため、専用のレイアウトと文字インデックスの構築が必要になる。
文字読み取りCAPTCHAは自動化が容易で強度は上がらないため、現時点では採用していない。

## ドキュメント

`/docs` はREADME.mdをその場でHTMLにして配信する(手書きのHTMLを持たない)。
検証は `node tools/verify_docs.mjs`(Markdown変換・目次リンク・エスケープ)と
`python tools/verify-docs.py`(実ブラウザでの描画・横スクロール・コンソールエラー)。
